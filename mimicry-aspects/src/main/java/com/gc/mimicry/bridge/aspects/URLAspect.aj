package com.gc.mimicry.bridge.aspects;

import java.net.MalformedURLException;

privileged aspect URLAspect
{
	// ------------------------------------------------------------------------------------------
	// Pointcuts
	// ------------------------------------------------------------------------------------------
	public pointcut ctor() : 
		call(java.net.URL.new(..)) && 
		!within(com.gc.mimicry..*) && 
		!within(java.net..*);

	// ------------------------------------------------------------------------------------------
	// Advices
	// ------------------------------------------------------------------------------------------
	
}
